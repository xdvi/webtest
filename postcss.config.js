module.exports = {
    plugins: [
      // Usar Tailwind CSS
      require('tailwindcss'),
      // Usar Autoprefixer para agregar prefijos de navegador automáticamente
      require('autoprefixer')
    ]
  }